.DS_Store
*.zip
node_modules/

# Secrets & data — MUST stay out of this PUBLIC repo. (The backend and the real
# client roster live in the private LongWave-Dev-Admin repo.)
.env
.env.*
!.env.example
*.local.json
*.sqlite
*.db

# Session-local Claude Code config (preview-server paths are machine-specific)
.claude/
