/* [build] 80-init.js  (bootstrap: applyLang/router/initReveal) - concatenated in load order by build.ps1; all parts share one IIFE scope */
  /* ---------------- init ---------------- */
  applyLang();
  router();
  initReveal();


